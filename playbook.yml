---
# Stuff with sudo
- hosts: all
  become: true

  roles:
    - role: system
      tags:
        - system
        - software_install

    - role: dotfiles
      tags:
        - dotfiles

    - role: geerlingguy.docker
      tags:
        - docker

  tasks:
    - import_tasks: tasks/mononoki.yml
      tags: software_install

# Stuff without sudo
- hosts: all

  roles:
    - role: rust
      tags:
        - software_install
        - rust
